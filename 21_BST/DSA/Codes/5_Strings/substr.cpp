#include<iostream>
#include <string>
using namespace std;
int main()
{
    string s="Animesh";

    // Substr:  string.substr(index,length)
    // cout<<s.substr(2);
    // cout<<s.substr(1,3);

    /*Input a string of even length and return the second
    half of that string using inbuilt substr function*/

    // string ques_1;
    // getline(cin,ques_1);
    // int size=(ques_1.size())/2;
    // cout<<ques_1.substr(size,ques_1.size());

    
 
    return 0;
}