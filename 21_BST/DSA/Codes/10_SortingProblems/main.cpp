#include<iostream>
#include<climits>
using namespace std;
int main()
{

    // LeetCode : 169

    //  int n=nums.size();
    //     sort(nums.begin(),nums.end());
    //     return nums[n/2];
 
    // LeetCode : 455

    /*
    int findContentChildren(vector<int>& g, vector<int>& s) {
          sort(g.begin(),g.end());
          sort(s.begin(),s.end());
          int count=0;
          int i=0,j=0;
          while(i<g.size()&&j<s.size()){
            if(s[j]>=g[i]){
                count++;
                i++;
                j++;
            }else{
                j++;
            }
          }
          return count;

    }
    */

 return 0;
}